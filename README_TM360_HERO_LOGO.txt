TM360° OFFICIAL - Homepage hero logo update

Upload these files directly into the GitHub repository root:
- site.js (replace the existing file)
- hero-logo.css
- TM360_OFFICIAL_logo.png (only if it is not already present)

The replacement keeps the existing fixed-header, mobile-menu and automatic-year behaviour.
On the homepage only, it replaces the TM360° wording inside the hero heading with the supplied TM360° logo.
No other wording or page is changed.

Commit the changes, allow Vercel to deploy, then hard-refresh with Ctrl+Shift+R.
